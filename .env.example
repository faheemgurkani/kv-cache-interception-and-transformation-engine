# HuggingFace authentication token (read access required for Qwen3-1.7B)
# Copy this file to .env and replace the placeholder:
#   cp .env.example .env
#
# Required for: scripts/download_model.py (local) and Modal secret huggingface-secret (HF_TOKEN)
# Reproducibility guide: docs/REPRODUCIBILITY.md
HF_TOKEN=your_huggingface_token_here
